{
  "navigationFallback": {
    "rewrite": "/login.html",
    "exclude": ["/api/*"]
  },
  "routes": [
    {
      "route": "/login",
      "rewrite": "/login.html"
    },
    {
      "route": "/logout",
      "redirect": "/login.html"
    }
  ]
}
